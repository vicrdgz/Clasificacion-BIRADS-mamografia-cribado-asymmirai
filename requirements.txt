# ================================================================================
# TFM - Cribado de mamografías a partir de representaciones latentes de AsymMirai
# Python 3.11 (recomendado, probado con miniconda)
# ================================================================================

# --- Core cientifico ---
numpy>=1.26
pandas>=2.0
scipy>=1.11
matplotlib>=3.7

# --- Machine learning clasico ---
scikit-learn>=1.5
xgboost>=2.0
lightgbm>=4.0

# --- Deep learning (PyTorch con soporte CUDA) ---
# Para CPU-only, instala directamente con pip.
# Para GPU CUDA 12.x, sigue las instrucciones en https://pytorch.org/get-started/locally/
# Ejemplo: pip install torch --index-url https://download.pytorch.org/whl/cu121
torch>=2.0

# --- Lectura DICOM (necesaria para la extraccion de features) ---
pydicom>=2.4
pylibjpeg>=2.0
pylibjpeg-libjpeg>=2.0
pylibjpeg-openjpeg>=2.0

# --- Jupyter ---
jupyter>=1.0
ipykernel>=6.0
nbformat>=5.9
notebook>=7.0

# --- Utilidades ---
tqdm>=4.65